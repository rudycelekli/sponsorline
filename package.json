{
  "name": "sponsorline-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "npm run build -w @sponsorline/core && npm run build -w sponsorline && npm run build -w @sponsorline/mcp",
    "test": "vitest run",
    "bench": "node bench/run.mjs"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0"
  }
}
